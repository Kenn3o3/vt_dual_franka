# @package _global_
defaults:
  - train_defaults
  - /agent_config: streaming_flow_matching_transformer_agent
  - process_batch_config: defaults
  - encoder_config: state_passthrough
  - override workspace_config: vector
  - _self_

predict_past: True

method_name: sfp-transformer

swanlab:
  group: streaming-flow-policy
